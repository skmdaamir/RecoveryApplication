package com.recoveryx.core.model.device;

/**
 * High-level storage device categories shown in the UI and used by scans.
 */
public enum DeviceCategory {
    INTERNAL_HDD,
    INTERNAL_SSD,
    EXTERNAL_HDD,
    EXTERNAL_SSD,
    USB_FLASH_DRIVE,
    MEMORY_CARD,
    VIRTUAL_DISK,
    UNKNOWN
}